# Session: 2026-05-08

## Objetivo
Crear reglas globales para el agente con flujo de trabajo Git, manejo de sesiones y convenciones del proyecto.

## Archivos Cambiados
- `.agents/rules.md` — Nuevo archivo con reglas del agente

## Comandos Ejecutados
```bash
git checkout -b agent/agent-rules
git add bfs-dispatch/.agents/rules.md
git commit -m "chore: add global agent rules with git workflow and session management"
git push -u origin agent/agent-rules
```

## Reglas Definidas
1. **Git Workflow** — Siempre crear rama `agent/<tarea>`, nunca commit directo a main
2. **Manejo de Sesiones** — Exportar sesión con `opencode export`, guardar resumen en `docs/agent-sessions/`
3. **Reglas Obligatorias** — Verificar rama limpia, crear rama antes de editar, ejecutar tests antes de commit
4. **Stack** — Next.js 16, shadcn/ui, Supabase, React Hook Form + Zod, Recharts
5. **Convenciones** — PascalCase componentes, camelCase utilidades, kebab-case rutas, snake_case DB

## PR
https://github.com/Brandoncm21/BSF-Dispacth/pull/new/agent/agent-rules
